# Local/Docker runtime dependencies.
#
# Modal uses requirements-modal.txt because llama-cpp-python needs a custom
# CUDA build step there.

-r requirements-base.txt

# GGUF inference. For local CPU/Docker installs, pip builds or installs the
# default llama-cpp-python wheel. Modal installs this separately with its
# prebuilt CUDA wheel.
llama-cpp-python==0.3.20
