# Shared runtime dependencies for the deployed FastAPI app.
#
# Keep this file to direct dependencies only. Transitive packages and CUDA
# support libraries should be resolved by pip/torch for the target platform.

fastapi
uvicorn[standard]

# Model artifact downloads from Hugging Face. Transformers 4.x requires
# huggingface_hub < 1.0.
huggingface_hub>=0.34.0,<1.0

# TTS inference.
coqui-tts==0.27.5
soundfile==0.13.1

# Torch runtime used by Coqui TTS and device checks.
torch==2.4.0
torchaudio==2.4.0

# Transformers 5.x expects newer PyTorch module APIs such as set_submodule.
# Keep this on 4.x while runtime torch is pinned to 2.4.0.
transformers==4.57.6
sentencepiece==0.2.1
safetensors==0.7.0
