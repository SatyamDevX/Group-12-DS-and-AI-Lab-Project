# Modal runtime dependencies before the CUDA llama-cpp-python build.
#
# Keep this self-contained. Modal's pip_install_from_requirements copies only
# this file into the image, so nested "-r requirements-base.txt" includes are
# not available during image build.

fastapi
uvicorn[standard]

huggingface_hub>=0.34.0,<1.0

coqui-tts==0.27.5
soundfile==0.13.1

torch==2.4.0
torchaudio==2.4.0

transformers==4.57.6
sentencepiece==0.2.1
safetensors==0.7.0

# Needed only when the frontend/API selects translation_model=llama_lora.
accelerate==1.13.0
bitsandbytes
peft==0.18.0
